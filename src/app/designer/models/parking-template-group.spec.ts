import { ParkingTemplateGroup } from './parking-template-group';

describe('ParkingTemplateGroup', () => {
  it('should create an instance', () => {
    expect(new ParkingTemplateGroup()).toBeTruthy();
  });
});
