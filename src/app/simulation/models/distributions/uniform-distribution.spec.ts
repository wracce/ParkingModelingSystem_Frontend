import { UniformDistribution } from './uniform-distribution';

describe('UniformDistribution', () => {
  it('should create an instance', () => {
    expect(new UniformDistribution()).toBeTruthy();
  });
});
