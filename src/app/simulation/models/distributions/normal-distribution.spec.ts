import { NormalDistribution } from './normal-distribution';

describe('NormalDistribution', () => {
  it('should create an instance', () => {
    expect(new NormalDistribution()).toBeTruthy();
  });
});
