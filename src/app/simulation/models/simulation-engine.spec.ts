import { SimulationEngine } from './simulation-engine';

describe('SimulationEngine', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new SimulationEngine()).toBeTruthy();
  });
});
